---
layout: 'default'
title: 'Lecture 01'
weburl: 'http://goo.gl/CYlgf'
units: []
---

include ../../fragments/slide_mixins

- var images = get_images('bees')

.deck-container
  // *notes This is a course about the limits of the world.  But let me start with a concrete example.
  mixin title_slide({document:document, images:images})

 
  //- section: unit_01 two pigs
  //- started translating to html
  //- include ../../fragments/unit_01_two_pigs
  - var d = get_unit('01')
    mixin render_unit(d)

  - d = get_unit('011')
    mixin section({unit:d, images:images})
    mixin render_unit(d)
    
  // *notes admin section
  section.slide
    img.bkg(src='/img/bkg/bees/DSC_0157_crop.jpg')
    .spacer &nbsp;
    .title-block: .title-container
        h2.title1 Admin
    .words: .container_12: .grid_12
      ul
        li.slide Seminar groups (check email or see module web page)
        li.slide Exercises
        li.slide Fast-track lectures 
          span#and_seminars.hide and seminars
          div.slide.anim-removeclass(data-what="#and_seminars",data-class="hide")
        li.slide Support
  // more about support --- the exams, bad email
  //- cut exams
    section.slide
      img.bkg(src='/img/lecture_01/slide-025.jpeg')
    section.slide
      img.bkg(src='/img/lecture_01/slide-026.jpeg')
    section.slide
      img.bkg(src='/img/lecture_01/slide-027.jpeg')
  // *notes bad email the night before resit exam
  section.slide
    img.bkg(src='/img/lecture_01/slide-031.jpeg')
  // *notes staircase, need to progress stepwise but we will go into the light
  section.slide
    img.bkg(src='/img/lecture_01/P4258786 merge2.jpg')


  //- unit: quick intro to Logiya
  - d = get_unit('02')
    mixin section({unit:d, images:images})
    mixin render_unit(d) 
  //- extension -- *todo incorporate in unit_02
  // *notes We need a formal language because ambiguity is awkward to deal with theoretically
  section.slide
    p.middle.center This is a hospital where doctors are trained.
  // *notes Appearance and reality. We need a formal language because we want a guarantee that a sentence which seems to express a proposition really does express a proposition.
  section.slide
    p.middle.center Many more people have been to Paris than I have.

  //- unit: logically valid arguments
  - d = get_unit('03')
    mixin section({unit:d, images:images})
    mixin render_unit(d)

  // *notes didn't do this in 2012-3 but should have, I think
  - d = get_unit('06')
    mixin section({unit:d, images:images})
    mixin render_unit(d)

  // *notes didn't do this in 2012-3 but leave it in in case you do have time!
  //- counterexamples & sound argument
  //- cut
    - d = get_unit('04')
      mixin section({unit:d, images:images})
      mixin render_unit(d)
    //- omit title for sound argument, treat it as part of counterexamples
    - d = get_unit('041')
      mixin render_unit(d)
     


