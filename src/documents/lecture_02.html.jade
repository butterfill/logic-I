---
layout: 'default'
title: 'Lecture 02'
weburl: ''
---

include ../../fragments/slide_mixins


.deck-container
  #title-slide.slide
    img.bkg(src='/img/bkg/sweeden/P8184971.JPG')
    .spacer &nbsp;
    .title-block: .title-container
        h1.title1 Logic I (PH126)
        h1.title2 #{document.title}
        h3.email s.butterfill@warwick.ac.uk
    if document.weburl
      .words
        span(style="position:relative;top:250px;left:450px;").white-box.big.center #{document.weburl}

 
  //- unit_013: learning objectives
  - each d in getCollection("documents").findAll({url:'/units/unit_013.html'},[{year:-1,sort_order:1}]).toJSON()
    != d.contentRendered



  // -------------------
  // *this might be in lecture 01!
  //- counterexamples & sound argument
  - each d in getCollection("documents").findAll({url:'/units/unit_04.html'},[{year:-1,sort_order:1}]).toJSON()
    section#unit_04.slide
      .words
        p.middle.center counterexamples
    != d.contentRendered

  //- unit (minor): sound argument
  section#unit_041.slide
    .words
      p.middle.center sound argument
  .slide
    div.white-box(style="width:230px;position:absolute;top:350px;left:500px;")
      p(style="padding: 0 0.3em;") An argument is 
        strong sound  
        | just if it is logically valid and all its premises are true.
    .slide
      div.white-box(style="width:230px;position:absolute;top:20px;left:500px;")
        p(style="padding: 0 0.3em;") An argument is 
          strong logically valid 
          | just if there's no possible situation in which the premises are true and the conclusion false.
    .slide
      .words
        div.middle.center
          table.fitch-proof
            tr
              td
                span.first-premise &#8203;
              td 1.
              td All humans are staplers.
              td
            tr
              td.last-premise
              td 2.
              td I am a human.
            tr
              td
                // span.bkg-grey-row &#8203;
              td 3.
              td I am a stapler.
      .slide.anim-addclass(data-what=".first-premise",data-class="bkg-grey-row")
      .slide.anim-removeclass(data-what=".first-premise",data-class="bkg-grey-row")
      .slide
        div.white-box(style="width:230px;position:absolute;top:20px;left:500px;")
          p(style="padding: 0 0.3em;") An argument is 
            strong logically valid 
            | just if there's no possible situation in which the premises are true and the conclusion false.



  - each d in getCollection("documents").findAll({url:'/units/unit_041.html'},[{year:-1,sort_order:1}]).toJSON()
    != d.contentRendered


  // -------------------
  // (could use to start if not starting this lecture with counterexapmles!)
  //
    //- recap: validity and counterexamples 
    - each d in getCollection("documents").findAll({url:'/units/unit_05.html'},[{year:-1,sort_order:1}]).toJSON()
      != d.contentRendered




  //- unit: sentence letters
  - each d in getCollection("documents").findAll({url:'/units/unit_012.html'},[{year:-1,sort_order:1}]).toJSON()
    section#unit_012.slide
      img.bkg(src='/img/bkg/sweeden/P8184972.JPG')
      .spacer &nbsp;
      .title-block: .title-container
          h2.title1 #{d.title}
    != d.contentRendered  
  
  //- unit: truth tables
  - each d in getCollection("documents").findAll({url:'/units/unit_071.html'},[{year:-1,sort_order:1}]).toJSON()
    section#unit_071.slide
      img.bkg(src='/img/bkg/sweeden/P8184975.JPG')
      .spacer &nbsp;
      .title-block(style="position:relative; top:425px"): .title-container
          h2.title1 #{d.title}
    != d.contentRendered

  //- postsecret quiz: logically valid arguments
  - each d in getCollection("documents").findAll({url:'/quizes/postsecret_01.html'},[{year:-1,sort_order:1}]).toJSON()
    != d.contentRendered


  //- unit: using truth-tables for validity
  //- preceeded by unit on formalizing the pigs
  //- (*todo : fix odd nesting, it's to use the title of the second unit)
  - each d0 in getCollection("documents").findAll({url:'/units/unit_135.html'},[{year:-1,sort_order:1}]).toJSON()
    - each d in getCollection("documents").findAll({url:'/units/unit_14.html'},[{year:-1,sort_order:1}]).toJSON()
      section#unit_135.slide
        img.bkg(src='/img/bkg/sweeden/P8184978.JPG')
        .spacer &nbsp;
        .title-block: .title-container
            h2.title1 #{d.title} 
        .slide.anim-attribute(data-what="img.bkg",data-attr="opacity:0.3")
          .words
           p.middle.center We can use truth tables to establish that 
             br
             | some arguments are logically valid
      != d0.contentRendered
      != d.contentRendered

  
  //- unit: complex truth tables, not(P and Q) 
  - each d in getCollection("documents").findAll({url:'/units/unit_60.html'},[{year:-1,sort_order:1}]).toJSON()
    section#unit_60.slide
      img.bkg(src='/img/bkg/sweeden/P8185094.JPG')
      .spacer &nbsp;
      .title-block: .title-container
          h2.title1 #{d.title}
    != d.contentRendered

  //- didn't do this in 2012-3
    //- unit: tautologies and contradictions, logical validity is not what you think it is.
    //- *todo would ideally save this for lecture 03! (ideally comes after unit_09!)
    - each d in getCollection("documents").findAll({url:'/units/unit_16.html'},[{year:-1,sort_order:1}]).toJSON()
      section.slide#unit_16
          img.bkg(src='/img/bkg/sweeden/P8195126.JPG')
          .spacer &nbsp;
          .title-block: .title-container
              h2.title1 #{d.title} 
      != d.contentRendered


    a.deck-permalink(href='.', title='Permalink to this slide') #
